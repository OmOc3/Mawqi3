rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Report photos are uploaded and read only through Admin SDK Server Actions.
      allow read, write: if false;
    }
  }
}
